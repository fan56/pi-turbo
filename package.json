{
  "name": "pi-opt",
  "version": "0.1.0",
  "description": "Optimized pi launcher — parallel extension loading with timing analytics",
  "type": "module",
  "bin": {
    "pi-opt": "./bin/pi-opt.js"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.50.0"
  }
}
